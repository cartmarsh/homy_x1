// Remove the following line if the imports are not used
// import { useState, useEffect } from 'react';

const useScrollDirection = () => {
    // Always return true to keep the header visible
    return true;
};

export default useScrollDirection;

